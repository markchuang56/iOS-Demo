//
//  UserProfileFromApp.m
//  FR_W310B
//
//  Created by h2Sync on 2017/11/3.
//  Copyright © 2017年 h2Sync. All rights reserved.
//

#import "H2AudioHelper.h"
#import "UserProfileFromApp.h"
/*
@implementation UserProfileFromApp

- (id)init
{
    if (self = [super init]) {
        _profile = [[SdkUserProfile alloc] init];
        
        _profile.uTag = 1;
        _profile.uBirthYear = BIRTH_YEAR;
        _profile.uBirthMonth = BIRTH_MONTH;
        _profile.uBirthDay = BIRTH_DAY;
        
        _profile.uGender = MALE;
        _profile.uBodyHeight = BODY_HEIGHT;
        
    }
    
    return self;
}

+ (UserProfileFromApp *)sharedInstance
{
    // initialize sharedObject as nil (first call only)
    static __strong id _sharedObject = nil;
    
    // structure used to test whether the block has completed or not
    static dispatch_once_t pred;
    // executes a block object once and only once for the lifetime of an application
    dispatch_once(&pred, ^{
        _sharedObject = [[self alloc] init];
    });

    return _sharedObject;
}

@end
*/
